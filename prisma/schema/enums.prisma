enum Role {
  ADMIN
  ORGANIZER
  USER
  MODERATOR
}

enum EventVisibility {
  PUBLIC
  PRIVATE
}

enum ParticipationStatus {
  PENDING
  APPROVED
  REJECTED
  BANNED
}

enum PaymentStatus {
  UNPAID
  PAID
  FAILED
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  DECLINED
}

enum Currency {
  USD
  BDT
  AED
  EUR
  GBP
}
